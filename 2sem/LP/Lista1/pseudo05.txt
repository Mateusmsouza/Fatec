algoritmo "maior_que_dez"
// Função :
// Autor :
// Data : 13/08/2018
// Seção de Declarações 
var
         A, B, C :real
		 
inicio
// Seção de Comandos 
   leia(A)
   leia(B)
   leia(C)
	
	se (A+B >= C e A + C >= B e B + C >= A) entao
		se (C == A e A == B e B == C) entao
			escreva("Triângulo equilátero")
		se (A == B ou C == A ou B == C) entao
			escreva("Triângulo Isósceles")
		se (A <> B e C <> A e B <> C) entao
			escreva("Triângulo Escaleno")
	senao
		escreva("Não dá pra ser Triângulo mano.")
	fimse
fimalgoritmo